<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Education - Update Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
    <link rel="stylesheet" href="style.css">
</head>
<body>

<%- include('partials/header') %>
<%- include('partials/sidebar') %>

<section class="form-container">
    <form action="/update-profile" method="post" enctype="multipart/form-data">
        <h3>Update Your Profile</h3>
        <% if (error) { %>
            <p style="color: red;"><%= error %></p>
        <% } %>
        <p>Your Full Name</p>
        <input type="text" name="name" placeholder="Enter your full name" value="<%= user ? user.name : '' %>" maxlength="100" class="box" required>

        <p>Your Email</p>
        <input type="email" name="email" placeholder="Enter your email" value="<%= user ? user.email : '' %>" maxlength="100" class="box" required>

        <p>Your Bio</p>
        <textarea name="bio" placeholder="Tell us about yourself" class="box" maxlength="500"><%= user && user.bio ? user.bio : '' %></textarea>

        <p>Update Profile Picture</p>
        <input type="file" name="image" accept="image/*" class="box">

        <input type="submit" value="Update Profile" name="submit" class="btn">
    </form>
</section>

<%- include('partials/footer') %>
<script src="/script.js"></script>
</body>
</html>